#include <iostream>
#include <cstdio>
#include <algorithm>
#include <cstdlib>
#include <cstring>
#include <string>
#include <vector>
#include <map>
#include <set>
#include <limits>
#include <iomanip>

using namespace std;

#define MIN(a, b) ((a < b) ? (a) : (b))
#define MAX(a, b) ((a > b) ? (a) : (b))

int main(){
//    freopen("maze.in", "r", stdin);
    int k_t;
    cin >> k_t;
    for(; k_t; k_t--)
    {
        string s;
        cin >> s;
        
    }
    return 0;
}